import { HomeIcon } from "@sanity/icons";
import { validation } from "sanity";

const home = {
	name: "home",
	title: "Home",
	type: "document",
	icon: HomeIcon,
 
	fields: [
		
	],
 
};

export default home;
